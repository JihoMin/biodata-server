args <- commandArgs(TRUE)
a <- as.array(args[1])
b <- args[2]

print(a[0])
print(b)

# dir <- getwd()
# dir <- paste(dir,"/src/rscripts/log_a_b.R", sep="")
# source(dir)

# val <- log_a_b(a,b)
cat('묘묘묘묘묘')